#### Variables for the install.sh script.
HOME_DIR="/opt"
LOGS_DIR="/opt/logs/wildfly"
DEV_HOME="/od"
ADMIN_HOME="/opt/admin"
SOFTWARE_HOME="${HOME_DIR}/software"

MAIN_MEDIA="/opt/media/wildfly-8.2.0.Final.zip"

HOSTNAME="app1.mustin.vm"
IP_ADDR="192.168.1.110"
SMTP_SERVER="mail.google.com"

JAVA_HOME="/usr/lib/jvm/jre-openjdk"
WILDFLY_USER="wildfly"
ADMIN_GROUP="wheel"

# Domain specific values
MASTER_OR_SECONDAYR="master"
SHORT_HOSTNAME="app1"

# (Optional) Patch List
PATCH_LIST=()

# Installation type, either 'standalone','domain_master', 'domain_slave'
INSTALL_TYPE="domain_master"

### LDAP related configurations
LDAP_URL="ldaps://support1.mustin.vm:636"
LDAP_ADMIN_GROUP="wildfly_admins"
LDAP_ADMIN_GROUP_DN="cn=wildfly_admins,cn=groups,cn=compat,dc=mustin,dc=vm"
LDAP_BIND_DN="uid=wildfly_bind,cn=sysaccounts,cn=etc,dc=mustin,dc=vm"
LDAP_NAME_ATTRIBUTE="uid"
LDAP_BASE_DN="dc=mustin,dc=vm"

# Vault related information
VAULT_ENC_FILE_DIR="vault"
VAULT_SALT="12345678"
VAULT_ITERATION_COUNT="100"
VAULT_ALIAS="vault"
